# Refuse to install if the running Node violates package.json "engines".
# Without this, engines is advisory and a wrong Node version fails later,
# at runtime, instead of at install time.
engine-strict=true
